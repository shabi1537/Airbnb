  <form action="/favourites/<%= home._id%>" method="POST" class="w-1/3">
    <input type="hidden" name="id" value="<%= home._id %>">
    <button type="submit" class="w-full bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-4 rounded transition duration-300">
      Remove
    </button>
  </form>